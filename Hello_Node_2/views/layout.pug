//- 주석(Comment)
//- 주석떄문에 pug파일이 html로 변환되는 과정에서
//- 문제를 일으킬떄가 있다.
//- layout.pug파일은 개발자가 직접 open을 하거나 핸들링하는 경우는 많지 않다.
//- 공통적으로 설정되는 header,link 등을 작성해두면
//- 다른 pug파일을 rendering할때 같이 묶어서 rendering을 수행할수 있다.

doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    //- block content 라고 설정을 해두면
    //- rendering 되는 pug파일에 같은이름의 block이 있어야 하고
    //- 파일을 rendering 할때 그 이름을 참조하여 한꺼번에 묶어서 연동을 해준다.
    //- 만약 undex.pug를 rendering하라는 콛가 있으면
    //- index.pug의 block content 위치 잇는 코드를
    //- layout.pug의 여기위치에 부착하여 하나의 html로 변환작업을 한다.
    block content
